#include<stdio.h>
int main ()
{
    //initializing through loop
    // int i, n;
    // printf("No of elements in an array: ");
    // scanf("%d", &n);
    // int spar[n];
    // for (i = 0; i < n;i++)
    // {
    //     printf("Enter the %d element ", i);
    //     scanf("%d", &spar[i]);
    // }
    // printf("The elements in the array are :\n");
    // for (i = 0; i < n;i++)
    // {
    //     printf("a[%d] is %d\n", i, spar[i]);
    // }


    //Ex-1 Write a program in C to read n number of values in an array and display them in reverwse order
    // int i, n;
    // printf("No of elements in an array: ");
    // scanf("%d", &n);
    // int spar[n];
    // for (i = 0; i < n;i++)
    // {
    //     printf("Enter the %d element ", i);
    //     scanf("%d", &spar[i]);
    // }
    // printf("The elements in the array in reverse order are :\n");
    // for (i = (n-1); i >= 0;i--)
    // {
    //     printf("a[%d] is %d\n", i, spar[i]);
    // }

    // //Ex-2 Write a program in C to find the sum of all the elements of the array
    // int i, n, sum=0;
    // printf("No of elements in an array: ");
    // scanf("%d", &n);
    // int spar[n];
    // for (i = 0; i < n;i++)
    // {
    //     printf("Enter the %d element ", i);
    //     scanf("%d", &spar[i]);
    //     sum = sum + spar[i];
    // }
    // // for (i = 0; i <n ;i++)
    // // {
    // //     sum = sum + spar[i];
    // // }
    // printf("The sum of elements in an array is : %d", sum);


    // //Ex-3 Write a program in C to copy the elements of one array into another array
    // int i, n, sum=0;
    // printf("No of elements in an array: ");
    // scanf("%d", &n);
    // int spar[n], spar2[n];
    // for (i = 0; i < n;i++)
    // {
    //     printf("Enter the %d element ", i);
    //     scanf("%d", &spar[i]);
    // }
    // for (i = 0; i <n;i++)
    // {
    //     spar2[i]=spar[i];
    // }
    // printf("The elements in the array2 are :   ");
    // for (i = 0; i < n;i++)
    // {
    //     printf("%d\t", spar2[i]);
    // }


    //Ex-3 Write a program in C to find max and min elements in the array
    // int i, n1, n2, max, min;
    // printf("No of elements in an array1: ");
    // scanf("%d", &n1);
    // int array1[n1];
    // printf("Enter the element for array 1 :\n");
    // for (i = 0; i < n1;i++)
    // {
    //     scanf("%d", &array1[i]);
    // }
    // max = array1[0];
    // min = array1[0];
    // for (i = 0; i < n1;i++)
    // {
    //     if (array1[i]>max)
    //         max = array1[i];
    //     else
    //     {
    //         if (array1[i]< min)
    //         {
    //             min = array1[i];
    //         }
    //     }
    // }
    // printf("Max in array 1 = %d\n", max);
    // printf("Min in array 1 = %d\n", min);


    //Ex-4 Write a program in C to separate even and odd
    // int i, n, j=0,k=0,l ;
    // printf("No of elements in an array1: ");
    // scanf("%d", &n);
    // int array1[n];
    // printf("Enter the element for array 1 : \n");
    // for (l = 0; l < n;l++)
    // {
    //     scanf("%d", &array1[l]);
    // }
    // int even[10], odd[10];
    // for (i = 0; i < n;i++)
    // {
    //     if (array1[i]%2==0)
    //     {
    //         even[j] = array1[i];
    //         j++;
    //     }
    //     else
    //     {
    //         odd[k] = array1[i];
    //         k++;
    //     }
    // }
    // printf("The even elements in array are :   ");
    // for (i = 0; i < j;i++)
    // {
    //     printf("%d\t", even[i]);
    // }
    // printf("\nThe odd elements in array are :   ");
    // for (i = 0; i < k;i++)
    // {
    //     printf("%d\t", odd[i]);
    // }


    // Ex-5 Write a program in C to insert a element in the unsorted array (incomplete)
    // int i, n;
    // printf("No of elements in an array: ");
    // scanf("%d", &n);
    // int spar[n];
    // for (i = 0; i < n;i++)
    // {
    //     printf("Enter the %d element ", i);
    //     scanf("%d", &spar[i]);
    // }  

    return 0;
}
