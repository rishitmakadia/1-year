#include<stdio.h>
int main()
{
    // Q-1) Write a program in C to display the cube of the number up to an integer
    // int num,i;
    // printf("Enter the number:");
    // scanf("%d", &num);
    // for (i = 1; i <= num;i++)
    // {
    //     printf("The cube of %d is %d\n", i, (i*i*i));
    // }

    // Q-2) Write a program in C to display the multiplication table for a given integer
    // int n,i;
    // printf("Enter the number:");
    // scanf("%d", &n);
    // for (i = 1; i <= 10; i++)
    // {
    //     printf("%d X %d = %d\n", n, i, (i * n));
    // }
    
    
    // Q-3) Write a C program to calculate the factorial of a given number  (long int)
    // long int n,i, fac=1;
    // printf("Enter the number:");
    // scanf("%ld", &n);
    // for (i = 1; i <= n; i++)
    // {
    //     fac = fac * i;
    // }
    // printf("Factorial of %ld is %ld", n, fac);


    // Q-4) Write a C program to find the sum of the series 1+11+111+----+n terms
    // int n, i, sum=0;
    // printf("Enter the number:");
    // scanf("%d", &n);
    // for (i = 1; i <= n;i++)
    // {
    //     sum = sum * 10 + i;
    // }
    // printf("the sum is %d ", sum);

    // Q-5) Write a C program to check wheter a given number is a 'Perfect' number or not
    // int n,i,sum=0;
    // printf("Enter the number: ");
    // scanf("%d", &n);
    // for (i = 1; i < n;i++)
    // {
    //     if (n%i==0)
    //         sum = sum + i;
    // }
    // if (sum==n)
    //     printf("%d is a perfect number", n);
    // else
    //     printf("%d is not a perfect number", n);


    // Develop a C program to print the sum of even numbers from M to N.
    // even , odd , even and odd
    // int m, n, i, sum;
    // printf("Enter the number m and n for range [m,n] :\n");
    // scanf("%d%d", &m, &n);
    // sum = 0;
    // for (i = m; i <= n; i++)
    // {
    //     if (i % 2 == 0)
    //     {
    //         sum = sum + i;
    //     }
    // }
    // printf("Sum of even number in [m,n] is = %d", sum);

    // Fibonassi Series 0112358
    // int n1=0, n2=1, n3, n, i;
    // printf("Enter the value of n : ");
    // scanf("%d", &n);
    // if (n<=0)
    //     printf("Error");
    // for (i = 1; i <= n;i++)
    // {
    //     printf("%d", n1);
    //     n3 = n2 + n1;
    //     n1 = n2;
    //     n2 = n3;
    // }
}