#include<stdio.h>
int main()
{
    // FILE *fptr;
    // char c;
    // fptr = fopen("BMSCE.txt", "w");
    // if (fptr==NULL)
    //     printf("Error\n");
    // printf("Read data from the keyboard : ");
    // while ((c = getchar()) != EOF)
    // {
    //     fputc(c, fptr);
    // }
    // fclose(fptr);
    // printf("\nData Output\n");
    // fptr = fopen("BMSCE.txt", "r");
    // while((c=fgetc(fptr))!=EOF)
    // {
    //     printf("%c", c);
    //     // fputs(fptr);    //error
    // }
    // fclose(fptr);


    // Write a program in C to read details of students from file and print Name, Roll No., Fees
    // FILE *fptr;
    // char c;
    // fptr = fopen("fav1.txt", "r");
    // if (fptr==NULL)
    //     printf("Error\n");
    // // while((c=fgetc(fptr))!=EOF)
    // // {
    // //     printf("%c", c);
    // // }
    // char line[10];
    // while (fgets(line, 15, fptr) != NULL)
    // {
    //     printf("%s", line);
    // }
    // fclose(fptr);

    // Write a program in C to count number of character, words and line in file.
    // FILE *fptr;
    // char c;
    // fptr = fopen("fav.txt", "r");
    // if (fptr==NULL)
    //     printf("Error\n");
    // int ch = 0, word = 0, line = 0;
    // while((c=fgetc(fptr))!=EOF)
    // {
    //     printf("%c", c);
    //     if (c)
    //     {
    //         ch++;
    //     }
    //     if (c==' ' || c=='\n')
    //     {
    //         word++;
    //     }
    //     if (c=='\n')
    //     {
    //         line++;
    //     }
    // }
    // printf("\n%d %d %d", ch, word, line);


    // char c;
    // c = getchar();
    // putchar(c);

    // int i;
    // printf("\nType text and press return to see it again \n");
    // printf("For exiting press <CTRL D> \n");
    // while((i=getchar( ))!=EOF) 
    //     putchar(i);
}